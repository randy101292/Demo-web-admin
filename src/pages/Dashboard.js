const Dashboard = () => {
  return (
    <span
      style={{
        justifyContent: "center",
        fontSize: "30px",
        display: "flex",
      }}
    ></span>
  );
};

export default Dashboard;
